# 用户偏好（全局）

> 此文件位于 `~/harness/user-memory.md`，跨所有项目共享。
> 记录你的通用偏好，所有 harness 项目启动时都会读取。
> 请按需填写，不需要的条目可以留空或删除。

## 代码风格

- 命名约定：（例如 camelCase / snake_case / PascalCase）
- 缩进风格：（例如 2 空格 / 4 空格 / Tab）
- 注释语言：（中文 / 英文）
- 最大行宽：（例如 80 / 100 / 120）
- 函数/方法长度偏好：（例如 不超过 50 行 / 不限制）

## 提交规范

- Commit message 语言：（中文 / 英文）
- 格式偏好：（Conventional Commits / 自由格式 / 其他）
- 是否需要 issue 关联：（是 / 否）

## 常用工具与命令

- 偏好的包管理器：（例如 npm / pnpm / yarn / pip / maven）
- 偏好的测试框架：（例如 jest / pytest / junit）
- 常用的验证命令：（例如 `npm test` / `mvn verify`）
- 常用的 lint 命令：（例如 `npm run lint` / `black .`）

## 回复与协作

- 回复语言：（中文 / 英文 / 跟随提问语言）
- 代码解释详细程度：（简洁 / 详细 / 仅关键处）
- 遇到不明确需求时的处理方式：（先问 / 先做合理假设再确认）

## 其他偏好

- 禁止使用的库或模式：
- 必须遵循的规范文档链接：
- 其他备注：
