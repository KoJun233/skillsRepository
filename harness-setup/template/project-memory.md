# 项目偏好

> 此文件位于 `./harness/project-memory.md`，仅当前项目生效。
> 记录本项目的特有约定和决策，所有在该项目的 agent 会话都会读取。
> 请按需填写，不需要的条目可以留空或删除。

## 项目信息

- 项目名称：{{PROJECT_NAME}}
- 创建日期：{{DATE}}
- 主要语言/框架：
- 仓库结构简述：

## 架构决策

- 核心架构风格：（例如 MVC / 分层架构 / Clean Architecture / 微服务）
- 关键依赖：（列出重要的第三方库及其用途）
- 已否决的方案及原因：

## 项目约定

- 模块划分规则：（例如按功能划分 / 按层划分）
- 文件命名规范：（例如 kebab-case / camelCase）
- API 设计规范：（例如 REST / GraphQL / RPC）
- 错误处理策略：（例如 统一异常类 / 错误码 / 抛原始异常）
- 日志规范：（例如 级别 / 格式 / 敏感信息处理）

## 测试约定

- 测试文件位置：（例如 `src/test/` / `tests/` / 同目录 `__tests__/`）
- 测试命名规范：
- 必须覆盖的场景：

## 特殊注意事项

- 已知坑点：（踩过的坑、奇怪的约束）
- 外部依赖约束：（例如 必须兼容某版本、内网环境限制）
- 不要触碰的模块/文件：
- 其他备注：
